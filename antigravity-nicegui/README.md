# antigravity-nicegui

A portfolio / demo single-page app built with [NiceGUI](https://nicegui.io/).

## Run

```sh
uv sync
uv run main.py
```

Then open the URL NiceGUI prints (defaults to <http://localhost:8080>).
